services:
  omniwatch:
    build: .
    ports:
      - "3456:3456"
    volumes:
      - omniwatch-data:/data
    environment:
      - NODE_ENV=production
      - OMNI_DATA_DIR=/data
    restart: unless-stopped

volumes:
  omniwatch-data:
